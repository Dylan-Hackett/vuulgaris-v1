# Vuulgaris V1 — KiCad project (main PCB)

**Migrated from EasyEDA 2026-08-16.** Schematic connectivity is generated from a
declarative netlist and verified against KiCad's own netlister. Requires KiCad 7.

```
vuulgaris.kicad_sch   69 components, 217 connections, 66 nets — VERIFIED 217/217
vuulgaris.kicad_pcb   284.3 x 125.0mm on Edge.Cuts
lib/                  symbols + footprints (see below)
tools/                the generator and the netlist it generates from
```

## Why this exists

The EasyEDA version was abandoned after its API repeatedly reported success for
writes it had not performed — `deleteBoard` returning true having deleted nothing,
`importChanges` returning true and doing nothing, wires created at exact pin
coordinates that the netlister then refused to bind. It produced a board where
**`P3V3_DAISY` was shorted to `I2C_SDA` across 9 pads** and both LDO outputs sat on
GND, and no ERC would flag any of it, because every one of those is an electrically
legal connection. It was only ever caught by diffing pad nets against an explicit
intended map.

KiCad's design is a text file. A write either happens or raises. That is the whole
reason for the move.

## The verification loop

This is the thing worth preserving. `tools/netmap.json` is the **intent**: every
pin that carries a net, taken from `docs/pin-allocation.md`. The check is:

```bash
python3 tools/mksch.py && python3 tools/netcheck.py
```

`netcheck.py` exports the netlist with `kicad-cli` — KiCad's own code, not a
reimplementation — and diffs every `(ref, pin) -> net` both ways. The reverse
direction is the one that matters: a pin KiCad bound to a net that `netmap.json`
never asked for is exactly the `P3V3_DAISY`-shorted-to-`I2C_SDA` failure above,
and no ERC will ever flag it. **Re-run it after any change.**

Then, before plotting fab files:

```bash
python3 tools/place.py --check && python3 tools/gerbercheck.py
```

## Libraries

| Library | Source | Notes |
|---|---|---|
| `lib/vuulgaris.kicad_sym` + `.pretty` | `easyeda2kicad` from LCSC part numbers | Same geometry as the EasyEDA parts, not a reinterpretation |
| `lib/daisy_es.kicad_sym` | [GregBurns/sm_kicad](https://github.com/GregBurns/sm_kicad) | Daisy Patch SM **plus** a Eurorack kit — jacks, toggles, pots, LEDs, power symbols |

Adding a part:

```bash
easyeda2kicad --full --output lib/vuulgaris --lcsc_id CXXXXXXX
```

**The Daisy symbol here has all 40 pins including A7 (GND).** The EasyEDA community
symbol was missing A7 entirely, which passes ERC silently — a pin that does not
exist cannot be reported unconnected. That defect does not exist in this project.

**Footprint format caveat:** easyeda2kicad writes the legacy `(module ...)` form.
KiCad converts it correctly when loading from the library, but it cannot be pasted
raw into a `.kicad_pcb`. Pull footprints in via Update PCB from Schematic, not by
editing the board file.

## Placement

Two kinds of position, governed differently:

| | Governed by | Move it in Pcbnew? |
|---|---|---|
| **Panel parts** (20) | the faceplate artwork | reverted, and reported — a knob has to come through its hole |
| **Free parts** (23) | you | kept. `place.py` re-reads them every run |

So: **drag the free parts around in Pcbnew, save, and they stay put.** Their
positions are recorded in `tools/free-placement.json` so the layout shows up in a
diff. A part missing from that file is new — it just arrived via F8 — and gets
seeded once from `FREE_SEED`, then the board owns it. `--reset` throws away free
moves and goes back to the seed.

Panel-facing parts are **derived, not chosen** — their coordinates come from
`hardware/placement-panel-facing.txt`, itself generated by
`mockups/generate-faceplate.py --placement`. KiCad PCB space is Y-down and native
millimetres, the same convention as that file, so the only transform is the origin
shift:

```
pcb_mm = panel_mm - (6.995, 7.000)
```

**Footprint origin is not the shaft.** What must line up with a faceplate hole is
the shaft axis, and for three of these parts that sits away from the origin:

| Part | Footprint | Shaft / centre at | Evidence |
|---|---|---|---|
| ENC1-ENC10 | `SW-TH_EC12EXXXX` | `(0, -3.75)` | silk body centre **and** both mounting lugs |
| ENC0 | `SW-TH_ALPS_EC11L1525G01` | `(0, -0.20)` | bushing circle r 4.00 |
| RV1 | `RES-ADJ-TH_RK09L1240A12` (dual-gang) | `(0, -4.83)` | bushing circle r 3.24 |
| RV2-RV4 | `RES-ADJ-TH_RK09D1130C4G` | `(0, -3.56)` | bushing circle r 2.50 |
| SW4-SW7 | `KEY-TH_CPG1511F01S0X` (Cherry MX) | `(0.63, 3.81)` | centre post, also a 4.2mm pad |
| DS1 | `LCD-TH_HS242L01W4S01` | header, mid of the left edge | (folded into `PANEL`) |

Placing those by origin puts the knob 4-5mm off its hole, which nothing downstream
would flag. `ORIGIN_OFFSET` in `place.py` carries the correction.

**Only ENC0 and the pots have a bushing circle to key off.** The EC12 has none —
its only circles are four r=0.9 pad drill markers, and treating the largest of
those as a shaft puts the part 4.1 x 5.1mm out. Infer the shaft from the largest
circle *above r 1.5*, the silk body centre, and the largest pad, and accept the
part if **any** of them lands on the hole. That is what `place.py` does.

Because `ORIGIN_OFFSET` is an input, checking board positions back against the
panel file cannot validate it -- both sides use the same table, so a wrong value
passes in both directions. `place.py` therefore re-derives each shaft from the
footprint's own geometry and reports disagreement. That check is what caught
ENC1-ENC10 sitting 3.75mm high after they had already been committed and pushed.

`place.py` also reports **footprint overlaps**, bounding each part by its own pad
and outline geometry. No ERC or DRC catches a part sitting on another at this
stage, and it already caught U5 landing on ENC6.

## Editing protocol

KiCad holds the design in memory and both **F8 and Save read from that snapshot,
not from disk.** An Eeschema window left open while these tools run will silently
overwrite them on its next save, and F8 from a stale window reports "no changes"
for edits that are sitting on disk. **Close Eeschema and Pcbnew between hand-offs.**

## Chassis lugs are grounded, deliberately

All 30 mounting tabs go to GND: `ENC0` pins 6/7, `ENC1`-`ENC8` pins D/E, `RV1`-`RV6`
pins 7/8. In each footprint these are the large slotted ovals set apart from the
signal pads, not signal pins -- note that on the EC11 (`ENC0`) it is **6 and 7**
that are the lugs, because **D and E there are the push switch**.

They are grounded rather than left open because this instrument reads
capacitance. A floating metal shell directly behind the touch panel is an
antenna. The solder joints also take the mechanical load off the signal pins.

**Consequence to watch at assembly:** the shells are bonded to the bushings, which
the nuts clamp to the faceplate. If the faceplate ever gets a ground pour near
those holes, main-board GND and faceplate GND are joined through the panel
hardware in parallel with J12's ground wires. That is usually fine and often
desirable, but it is a second path and worth knowing about before chasing a hum.

## kpins.json is generated, untracked, and easy to generate WRONG

`tools/kpins.json` is the pin-geometry cache `mksch.py` reads. It is not in git.
Rebuild it with the exact argument list in `ksym.py`'s docstring -- **project
libraries last, and never `lib/*.kicad_sym`**.

**Why the list is explicit rather than a glob:** `lib/daisy_patch_sm.kicad_sym`
was never in `sym-lib-table` but held a second, different
`ES_DAISY_PATCH_SM_REV1` -- A6 at x -5.08 instead of -1.27, A10 at -7.62 instead
of -6.35. Including it made mksch.py draw the power stubs in the wrong place;
`U1.A6` and `U1.A10` silently stopped connecting on a schematic that looked
correct, and netcheck caught it as `unconnected-(U1-+5V-PadA6)`. **That file is
deleted as of 2026-09-07** (committed in 398a1f2, referenced by nothing), but
any duplicate symbol name will do the same thing, so keep the argument list
explicit.

Running `ksym.py` with no arguments writes an empty cache and mksch.py dies on a
KeyError.

## Still open (unchanged by the migration)

- ~~**The LPG.**~~ **DONE 2026-09-06** — stereo Bergman LPG is in `netmap.json` and on the board. See [lpg-bergman.md](../../docs/lpg-bergman.md).
- **Jack pinout.** `PJ-603` (J7-J10) **RESOLVED 2026-09-06** from the Legion/Haoyu
  drawing behind LCSC C41409498: **2 = sleeve, 4 = tip, 3 = normalling switch onto
  the tip, 5 = ring.** The drawing's SCHEMATIC box settles it -- the mono variant
  `PJ-603-3` keeps 2/3/4 and drops 5, so 5 is the ring -- and the footprint agrees,
  its pads 3 and 4 sharing y = +2.50 as the switch pair with 5 opposite.
  **`PJ-376` (J2-J5) RESOLVED the same day** from LCSC C22355746's drawing:
  **1 = sleeve, 2 = ring, 3 = tip, and no switch contact** -- its CIRCUIT DIAGRAM
  shows three independent contacts, where `PJ-603` has a fourth riding on the tip.
  The plug drawing numbers the segments 1/2/3 sleeve-to-tip and the PCB layout puts
  pin 1 nearest the barrel; the footprint agrees, pad 1 at x -3.50 on the bushing
  side. **All eight jacks are now wired.**
- ~~**470uF bulk cap** at the OLED rail~~ — **STRUCK 2026-09-08, it was never a
  requirement.** Traced to commit 600efda: the 470uF was `25YXJ470M10X12.5`
  (C88740), the **input bulk cap for the rejected barrel-jack power scheme**
  (12V barrel + B1212S, decided 2026-08-20 and replaced by USB-C + DKM10E-12).
  Nothing to do with the OLED rail, and `design-state` §5.5 no longer exists.
  Current input bulk is `C29`, 22uF/50V. The OLED rail has `C24` 10uF in, `C20`
  10uF + `C21` 100nF out, which is normal for an AMS1117 feeding a 40mA panel.
- ~~**main PCB mounting holes**~~ — **there are none, by decision.** `design-state`
  "Panel mounting" (2026-09-05): the board has no standoffs of its own and hangs off
  the panel hardware. Load path is enclosure -> faceplate -> nuts -> bushings -> part
  bodies -> **solder joints** -> main PCB.
- **Jack panel positions.** J2-J5 and J7-J10 have board positions in
  `free-placement.json` and the counterbore geometry is in `design-state` §11, but
  nothing states where they land on the **enclosure wall**, which is a different part
  from the faceplate.

### The board is located by two different parts at once

With no mounting holes, the main PCB's position is set entirely by parts soldered to
it, and those parts reference **two different things**:

| | clamped to | sets |
|---|---|---|
| 6 pot bushings, nutted | the **faceplate** | height below the panel (10mm) |
| 8 jack barrels, nutted | the **enclosure wall** | position along the top edge |

The faceplate screws to the enclosure at the cheeks, so these are one rigid
assembly rather than two floating parts — which makes this a **tolerance stack, not
a mechanism**. But the board still has to satisfy both references simultaneously,
and it has no compliance: any disagreement is taken up by bending FR4 and loading
the solder joints that `design-state` already names as the load path.

**This shows up at first fit-up, not on screen.** The thing to check on the first
enclosure is whether the wall's jack holes and the faceplate's bushing holes agree
on where the board sits, to better than the boards can flex. If they do not, the
cheap fix is to stop nutting the jacks and let them float in oversized wall
holes — accepting that a hard cable pull then loads solder joints, which is the
trade `design-state` §11 was already weighing for a different reason.
